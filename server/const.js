// Informazioni del database
const DB_HOST = "database";
const DB_PORT = 3306;
const DB_USER = "think";
const DB_PASSWORD = "root";
const DB_DATABASE = "think";

// Altre costanti
const APP_PORT = 3000;

// Esporta le costanti
module.exports = {
  DB_HOST,
  DB_PORT,
  DB_USER,
  DB_PASSWORD,
  DB_DATABASE,
  APP_PORT
};